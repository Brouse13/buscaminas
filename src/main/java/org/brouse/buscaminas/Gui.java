package org.brouse.buscaminas;


public class Gui{
    /*
    List<JButton> buttons = new ArrayList<>();

    public Gui() {
        this.setTitle(Settings.title);
        this.setSize(Settings.min_width, Settings.min_height);
        this.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        this.setResizable(false);
        this.setLayout(null);

        drawFields(Settings.min_width, Settings.min_height);
        for (JButton button : buttons) {
            this.add(button);
        }

    }

    private void drawFields(int currentWidth, int currentHeight) {
        for (int x = 0; x < Settings.size; x++) {
            for (int y = 0; y < Settings.size; y++) {
                JButton button = new JButton();
                int setx = currentWidth/Settings.size*x;
                int sety = currentHeight/Settings.size*y;

                button.setBounds(currentWidth/Settings.size*x, currentHeight/Settings.size*y,currentWidth/Settings.size, currentHeight/Settings.size);
                button.setToolTipText("fds");
                buttons.add(button);
            }
        }
    }
    * */
}
